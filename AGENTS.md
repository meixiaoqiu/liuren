# 仓库工作规则

## Git 安全

- 除非用户在当前请求中明确授权创建某个具体提交，否则绝对不得创建 Git 提交。
- 不得从“实现”“完成”“测试”“准备”“清理”或“为发布做好准备”等请求中推断用户已经授权提交。
- 默认保持已完成的修改处于未提交状态，并向用户报告工作区状态。
- 除非用户明确授权对应的具体 Git 操作，否则不得暂存文件、修改提交、改写历史、创建或移动标签、合并、变基、重置分支或推送远程仓库。
- 在执行任何已经获得授权的提交之前，必须先向用户展示准备提交的文件集合和提交信息，并等待用户确认。

## README 归属

- 绝对不得修改 `README.md`，该文件由用户亲自维护。

## 终审与安全扫描

- 用户要求“终审”时，默认审查当前全部待提交内容，包括代码正确性、回归保护、测试、格式、敏感信息及明显安全风险。
- 是否使用 Codex Security 应根据变更涉及的安全风险判断，不得将其作为每次终审的必选步骤。
- 仅涉及排盘计算、规则判断、结果展示文案或回归测试的修改，且没有改变外部输入处理、权限或数据访问边界时，默认不运行专项安全扫描。
- 涉及登录认证、授权、会话、用户输入处理、数据库查询、动态页面输出、文件操作、上传、外部请求、命令执行、依赖或部署安全配置时，应评估并说明是否需要专项安全扫描。
- 用户明确要求安全扫描时，必须执行对应流程；无法完成时如实报告原因。
- 未运行或未完成专项安全扫描，不应单独作为终审不通过或不可提交的理由；已发现且未解决的实际风险仍需报告。
- 必须分别报告代码审查、测试与专项安全扫描状态；不得将测试通过表述为安全扫描通过。
- 本规则不改变任何基线批准要求，也不构成暂存、提交或推送授权。

## PHP 与 Docker 运行环境

- 宿主机和 WSL 中均不提供 PHP 运行时；绝对不得直接在宿主机或 WSL 中运行 `php`、`composer`、`php artisan`、Pest 或 PHPUnit 命令。
- 所有 PHP、Composer、Artisan、Pest 和 PHPUnit 命令都必须在项目的 Docker 容器内运行。
- 项目 PHP 容器名为 `php85-fpm`，容器内仓库目录为 `/var/www/html/liuren`。例如，运行 Artisan 测试应使用 `docker exec -w /var/www/html/liuren php85-fpm php artisan test`。
- 需要执行 PHP 相关命令时，应先检查当前运行的 Docker 容器以确定项目容器和进入方式；只有在无法从环境中确定时才询问用户，不得改用宿主机 PHP。

## 排盘计算回归安全

以下规则适用于任何可能影响排盘结果的修改。

- 核心基线为12种天地盘关系 × 60个日干支，共720课；课号不带昼夜后缀。十二天将和三传天将共15个字段是依赖占时的动态变量，不进入核心基线。
- 修改排盘计算逻辑之前，必须先运行现有的720核心课回归测试，并确认测试通过。
- 绝对不得直接编辑、重新生成或整体替换 `tests/Fixtures/pan_regression_720.json`。
- 初始基线创建完成后，绝对不得再次运行 `pan:regression-initialize`。
- 修改排盘计算逻辑后，必须运行 `tests/Feature/PanRegressionTest.php`。
- 天将必须由 `tests/Feature/PanNoblemanTest.php` 独立保护，覆盖十干的昼贵与夜贵、北京固定UTC+8的日出日落切换（边界前一秒、当秒及后一秒）、贵人落十二地盘宫的顺逆布将、完整十二天将排列及三传所乘天将。
- 天将测试必须使用独立的规则表或明确的预期值，不得调用待测昼夜算法生成预期值；变更天将逻辑后必须运行独立天将测试、720核心回归和完整测试套件。
- 独立天将测试的预期输出变化也必须展示精确用例、全部变化字段及前后值，并获得用户明确批准；不得通过删除断言或缩小覆盖让测试通过。
- 测试报告中的所有变化课默认均为“尚未批准”。
- 必须通过修正排盘逻辑消除所有非预期变化，绝对不得仅为了让测试通过而批准非预期变化。
- 请求用户批准之前，必须向用户展示每个变化课的精确课号、全部变化字段以及各字段修改前后的值。
- 只有用户明确批准了对应的具体课号及字段变化后，才可以更新这些课的基线。
- 更新基线时只能使用 `pan:regression-approve`，并且只能列出已经获得用户明确批准的精确课号。
- `pan:regression-approve` 必须始终要求人工确认，绝对不得增加或使用任何绕过确认的机制。
- 完成批准后，必须重新运行720核心课回归、独立天将测试和完整测试套件。
- 必须区分720核心回归、独立天将测试与安全扫描的状态；不得将720回归表述为原1440回归通过，安全扫描未完成时不得宣称扫描通过。
- 完成工作后必须报告所有工作区变更；未经明确授权，不得暂存或提交这些变更。
